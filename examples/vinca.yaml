ros_distro: melodic

# mapping for package keys
conda_index:
  - conda-forge.yaml
  - packages-ignore.yaml

skip_all_deps: false

packages_skip_by_deps:

packages_select_by_deps:
  #  - rviz
  - ros_core
  - actionlib
  - dynamic_reconfigure

patch_dir: patch
