ros_distro: noetic

# mapping for package keys
conda_index:
  - conda-forge.yaml
  - packages-ignore.yaml

skip_all_deps: true

packages_skip_by_deps:

packages_select_by_deps:
  - rviz
  # - python_qt_binding
  # - urdf_parser_plugin
  # - urdf
  # - ros-core
  # - actionlib
  # - dynamic_reconfigure

patch_dir: patch
